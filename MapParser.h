#pragma once
#include <map>
#include <string>
#include <string>
#include <tinyXML/tinyxml.h>
class MapParser
{
	private:
		std::string textureId;
		int scale;
		int tileSize;

	public:
		Map(std::string)
};

